server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dsr_portal
    username: root
    password: 636388
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  # ❌ TEMP: disable redis (it can crash app if not running)
  # redis:
  #   host: localhost
  #   port: 6379

  # ❌ TEMP: disable mail (VERY IMPORTANT - common crash reason)
  # mail:
  #   host: smtp.gmail.com
  #   port: 587
  #   username: svinutaareddy@gmail.com
  #   password: bmpaxhzqqinrnqsc
  #   properties:
  #     mail:
  #       smtp:
  #         auth: true
  #         starttls:
  #           enable: true

jwt:
  secret: mysecretkey
  expiration: 3600000

logging:
  level:
    org.springframework: INFO